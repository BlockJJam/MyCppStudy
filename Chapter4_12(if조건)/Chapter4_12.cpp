#include <iostream>

using namespace std;


int min(int x, int y)
{
	return ( x > y ) ? y : x;
}

int main()
{
	int x, y;
	cin >> x >> y;
	min(x, y);

	if (x = 0)
		cout << x << endl;
	cout << x << endl;

	return 0;
}